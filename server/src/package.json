{
  "name": "app",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "start": "if test \"$NODE_ENV\" = \"production\"; then node ./bin/www ; else nodemon ./bin/www ; fi"
  },
  "dependencies": {
    "carbone": "^2.1.1",
    "cors": "^2.8.5",
    "express": "~4.18.2",
    "formidable": "^1.2.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.6"
  }
}
